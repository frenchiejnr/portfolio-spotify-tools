{
    "configurations": [
        
    {
        "name": "Launch Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "http://localhost:5173",
        "webRoot": "${workspaceFolder}/frontend",
          "skipFiles": [
     "<node_internals>/**/*.js", "${workspaceFolder}/**/node_modules/**/*.js"
  ]
    }
    ]
}